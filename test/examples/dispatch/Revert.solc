import std;
import dispatch;

function my_revert() -> word {
  revert("regression");
  return 0;
}

contract Foo {
  constructor() {}

  function noAnswer() -> uint256 {
    return uint256(my_revert());
  }

  function answer() -> uint256 {
    return uint256(42);
  }
}

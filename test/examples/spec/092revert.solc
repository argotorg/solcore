
function revert(reason) -> word {
  let res: word;
  assembly {
    mstore(0x0,reason)
    revert(0,32)
  }
}


contract Revert {
  function main() {
    revert("Não sei, o meu coração");
    return 7;
  }
}

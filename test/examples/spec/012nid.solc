contract Id1 {
  forall a . function id(x : a) -> a {
    return x ;
  }

  forall a . function nid() -> ((a) -> a) {
    return id;
  }

  forall a b . function const(x : a, y : b) -> a { return x; }

  function main() -> word {
    return const(nid(42), id(1));
  }
}

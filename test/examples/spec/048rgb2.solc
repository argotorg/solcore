contract RGB {
  data Color = R | G | B;

  function fromEnum(c : Color) -> word {
    match c {
      | R => return 4;
      | G => return 2;
      | B => return 42;
    }
  }

  function main() -> word { return fromEnum(B); }
}

contract RGB {
  data Color = R | G | B;

  function fromEnum(c) {
    match c {
      | Color.R => return 4;
      | Color.G => return 2;
      | Color.B => return 42;
    }
  }

  function main() { return fromEnum(Color.B); }
}

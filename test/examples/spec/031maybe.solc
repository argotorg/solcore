contract Option {
  data Option(a) = None | Some(a);

  function just(x) { return Option.Some(x); }

  function maybe(n, o) {
    match o {
      | Option.None => return n;
      | Option.Some(x) => return x;
    }
  }

  function main() {
    return maybe(0, Option.Some(42));
  }
}

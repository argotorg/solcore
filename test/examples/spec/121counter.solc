// test single contract field
// import StorageLib;
import NumLib;
import assign;

contract Counter {
  counter : word;

  function main() -> word {
    counter = Num.add(counter, 1);
    return counter;
  }
}

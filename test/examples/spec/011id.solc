contract Id1 {

  data Bool = False | True;

  forall a . function id(x : a) -> a {
    return x ;
  }

  forall a b . function const(x : a, y : b) -> a { return x; }

  function main() -> word {
    return const(id(42), False);
  }
}

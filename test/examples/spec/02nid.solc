contract Id1 {
  function id(x) {
    return x ;
  }

  function nid() -> (word) -> word {
    return id;
  }

  function const(x, y) { return x; }

  function main() {
    return const(nid(42), id(1));
  }
}

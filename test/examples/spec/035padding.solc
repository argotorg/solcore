contract Option {
  data Option(a) = None | Some(a);

  forall a . function maybe(n : a, o : Option(a)) -> a {
    match o {
      | Some(x) => return x;
      | None => return n;
    }
  }

  function main() -> word {
    return maybe(7, None);
  }
}

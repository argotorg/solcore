contract Option {
  data Option(a) = None | Some(a);

  function maybe(n, o) {
    match o {
      | Option.Some(x) => return x;
      | Option.None => return n;
    }
  }

  function main() {
    return maybe(7, Option.None);
  }
}

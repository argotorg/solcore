data Bool = False | True;

  function second(x, y) {
    match x, y {
    | True, z => return z;
    | False, z => return z;
    }
  }

contract Second {
  function main() -> word {
    second(True, 42)
  }
}

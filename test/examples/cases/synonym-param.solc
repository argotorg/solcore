type MyPair<a, b> is pair<a, b>;
type IntPair is MyPair<word, word>;

function makePair(x: word, y: word) returns (MyPair<word, word>) {
    return pair(x, y);
}

function main() returns (word) {
    let p: IntPair = makePair(42, 100);
    match (p ) {
        case pair(x, _) { return x;
    } }
}

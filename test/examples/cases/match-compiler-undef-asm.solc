data Foo(a) = Foo(word);

function read(x) {
  let res : word;
  match (x) {
  | Foo(w) =>
    assembly {
      res := w
    }
  }
  return res;
}

contract Bla {

  function main () -> word {
    return read(Foo(42));
  }
}

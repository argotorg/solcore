data Bool = False | True;

contract CatchAll {
    function catchAll(x, y) -> Bool{
      match x, y {
      | True, True => return True;
      | z, w      => return z;
      }
    }

  function main() -> Bool {
    catchAll(True, False)
  }
}

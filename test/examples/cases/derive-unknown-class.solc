import std.{*};
import std.Generic.{*};

pragma no-patterson-condition;
pragma no-bounded-variable-condition;

#[derive(NoSuchClass)]
data Color = Red | Green | Blue;

function useIt() -> bool {
    return true;
}

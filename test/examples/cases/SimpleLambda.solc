contract SimpleLambda{
  function f (z) {
    let n = lam (x,y) {
      return primAddWord(x,primAddWord(y,1));
    } ;
    let m = lam (x) {
      return primAddWord (z,x) ;
    } ;
    let p = m();
    return p(n(1,0));
  }
  function main() {
    return f(50);
  }
}

contract SimpleLambda{
  function f (z : word) {
    let n = lam (x,y) {
      return primAddWord(x,primAddWord(y,1));
    } ;
    let m = lam (x) {
      return primAddWord (z,x) ;
    } ;
    return n(1,0) : word;
  }
  function main() {
    return f(50);
  }
}

contract SimpleLambda{
  function f (z : word) {
    let n = lam (x) {
      return primAddWord(x,1);
    } ;
//    let m = lam (x) {
//      return primAddWord (z,x) ;
//    } ;
    return n(0) : word;
  }
}

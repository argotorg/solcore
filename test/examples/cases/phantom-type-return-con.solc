data Foo(a) = Foo(word);
  function wrap(x) {
    return Foo(x);
  }

  function unwrap() -> word {
    match(wrap(42)) {
    | Foo(w) => return w;
    }
  }

  contract C {
    function main() -> word {
      return unwrap();
    }
  }

data Bool = False | True;

function test(v0, p) {
  match p {
  | True => return False;
  | z    => return v0;
  }
}

contract FreshVariableShadowing {
  function main() -> Bool {
    test(True, False)
  }
}

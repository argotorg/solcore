// Shared storage identity.

export { AppStore, appStore };

data AppStore = AppStore;

// A value-level handle for the tag, so call sites write `appStore()` instead of
// the nullary-constructor dance.
function appStore() -> AppStore {
  let s : AppStore = .AppStore;
  return s;
}

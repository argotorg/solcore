// Shared storage identity (see the ERC20 example for the rationale).

export { AppStore, appStore };

data AppStore = AppStore;

function appStore() -> AppStore {
  let s : AppStore = .AppStore;
  return s;
}
